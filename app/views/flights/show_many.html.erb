<!-- multiple flight select option (version B) -->
<div id="logo"><img src="/images/freezefare-logo.png" /></div>

<div class="main">
<div style="font-size:12pt">
<table width="100%">
<tr><td>From: </td><td>Departing On:</td></tr>
<tr><td><%= @flight.from_code %></td><td>
<%= @flight.from_date.strftime("%m/%d/%Y")%> </td></tr>

<tr><td>To:</td><td>Arriving At:</td></tr>
<tr><td> <%= @flight.to_code %></td><td><%= @flight.to_date.strftime("%m/%d/%Y") %>
</td></tr>
</table>
</div>

<% form_for(@flight_select) do |f| %>
  <%= f.error_messages %>

       <div class="content">Freeze this fare for: <br /><%= image_submit_tag '/images/freezefare-5bucks-btn.gif'%>  <%= image_submit_tag 'freezefare-10percent-btn.gif' %></div>
       
   <br />
   <div style="font-size:10pt">
<table border=0>
<tr>
<th></th>
<th>Price</th>
<th>Airline</th>
<th>Flight</th>
</tr>
<%    @xml.elements.each("/searchresult/trips/trip") do |e| %>
<tr>
<td>   	<%= f.radio_button :kayak_flight_id, :value=>e.attribute('id') %>     </td>   
        <% e.each_element("price") do |t|   %>       
	         <%=t.attribute("id") %>        <td> $<%=t.text%></td>
	
        <% end 
		e.elements.each("legs/leg/segment/airline") do |a|
			%><td><%=a.text %></td>
	<%	end 
		e.elements.each("legs/leg/segment/flight") do |af|
			%><td><%=af.text %></td>
	<%	end %>
	 </tr>
	</p>
     <% end %>
</table>
</div>
       <div class="content">Freeze this fare for:<br /> <%= image_submit_tag '/images/freezefare-5bucks-btn.gif'%>  <%= image_submit_tag 'freezefare-10percent-btn.gif' %></div>
<% end %>
</div>